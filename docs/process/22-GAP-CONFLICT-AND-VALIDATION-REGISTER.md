# 22. Gap, conflict and validation register

> **Generated.** Produced by `scripts/process-docs.mjs` from `docs/reference/process-inventory.json`,
> which `scripts/process-discovery.mjs` builds by reading the artifacts in this repository.
> Do not edit this file: edit the artifact, or the script that reads it, and regenerate.
> Inventory generated 2026-08-28. Standard `dgo-process-documentation/v2`.

**Purpose.** Record every material gap with the fifteen attributes the standard requires, the ownership type missing, and the authoritative source that would close it.

28 gaps. 16 at high validation priority.

## High priority — 16

| ID | System | Module | Process | Step | Gap | Missing information | Available evidence | Classification | Why required | Impact of absence | Operational risk | Authoritative source | Ownership type missing | Resolution criteria | Status | Sources |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| GAP-001 | DGO Internal Platform | config/action-ownership.config.js and config/module-boundaries.config.js | — | — | Two artifacts authorise a governed action, and only one of them says what the action does | A per-action governance record for every name the runtime will accept, and a stated precedence between the two authorities. | core/action-authority.js accepts an action when the per-action table carries a spec for it OR the module boundary charter lists it in owns[]. The table carries 61 specs, each naming an owner, a service, an audit vocabulary and a backend requirement. The charter declares 124 capability names across 29 modules, of which 82 have no per-action spec. The charter list mixes writes with view capabilities, so its entries cannot be read as actions without checking each one. | Conflicting | An action authorised only by the charter runs with no declared service, no declared audit vocabulary and no declared backend requirement, while an action in the table runs with all four. Nothing states which authority governs when they disagree. | Governed writes exist that this documentation can name but cannot describe: the FastTrack escalation and owner-notification actions are authorised by the charter alone and carry no audit vocabulary of their own. | A write reaches the system of record without the audit event a reader of the governance table would expect it to write. | The platform technical owner, as the owner of both configurations. | Technical owner | Every name the runtime will accept as an action has a per-action record, or the charter states explicitly which of its entries are actions and which are views. | Open | SRC-035 SRC-005 SRC-037 |
| GAP-003 | DGO Internal Platform | core/lifecycle.js | Internal correspondence lifecycle | — | Lifecycle state 'duplicate' has no declared successor | Either an outgoing transition, or a statement that this state is intentionally terminal. | 'duplicate' is the target of a transition from arrival, registered and declares no outgoing transition. | Confirmed | A record entering this state cannot lawfully progress or be reopened through the guard. | Work reaching this state stops there with no evidenced route out. | Records may be operationally stranded, with no path the software will permit. | The process owner for the correspondence lifecycle. | Process owner | The transition map declares an outgoing edge, or a written statement records the state as intentionally terminal. | Open | SRC-046 |
| GAP-004 | DGO Internal Platform | core/lifecycle.js | Internal correspondence lifecycle | — | Lifecycle state 'on_hold' has no declared successor | Either an outgoing transition, or a statement that this state is intentionally terminal. | 'on_hold' is the target of a transition from arrival, registered and declares no outgoing transition. | Confirmed | A record entering this state cannot lawfully progress or be reopened through the guard. | Work reaching this state stops there with no evidenced route out. | Records may be operationally stranded, with no path the software will permit. | The process owner for the correspondence lifecycle. | Process owner | The transition map declares an outgoing edge, or a written statement records the state as intentionally terminal. | Open | SRC-046 |
| GAP-005 | DGO Internal Platform | core/lifecycle.js | Internal correspondence lifecycle | — | Lifecycle state 'reassign_requested' has no declared successor | Either an outgoing transition, or a statement that this state is intentionally terminal. | 'reassign_requested' is the target of a transition from assigned and declares no outgoing transition. | Confirmed | A record entering this state cannot lawfully progress or be reopened through the guard. | Work reaching this state stops there with no evidenced route out. | Records may be operationally stranded, with no path the software will permit. | The process owner for the correspondence lifecycle. | Process owner | The transition map declares an outgoing edge, or a written statement records the state as intentionally terminal. | Open | SRC-046 |
| GAP-006 | DGO Internal Platform | core/lifecycle.js | Internal correspondence lifecycle | — | Lifecycle state 'rejected' has no declared successor | Either an outgoing transition, or a statement that this state is intentionally terminal. | 'rejected' is the target of a transition from arrival, registered, pending_review, escalated and declares no outgoing transition. | Confirmed | A record entering this state cannot lawfully progress or be reopened through the guard. | Work reaching this state stops there with no evidenced route out. | Records may be operationally stranded, with no path the software will permit. | The process owner for the correspondence lifecycle. | Process owner | The transition map declares an outgoing edge, or a written statement records the state as intentionally terminal. | Open | SRC-046 |
| GAP-007 | Both platforms | config/status-vocabulary.config.js | Public status presentation | — | Internal status 'Accepted' maps to public 'review' on a reading, not a decision | An agency decision on what this internal status means to a citizen. | The mapping is declared, and its own source records the ambiguity and the alternative reading: "Accepted" means the registry accepted the item for handling, which is the public "Under review". If it instead means the request itself was granted, it maps to "approved". | Conflicting | A citizen reading the public label and an officer reading the internal one must be looking at the same thing. | A citizen may be told a matter is at a stage the agency does not consider it to be at. | Misinformed applicants, and complaints founded on a label the agency never intended. | The registry owner. The source itself names docs/audits/DESIGN_AUDIT_BRIEF_ASSESSMENT.md as the point of confirmation. | Business owner | The agency confirms the mapping, or supplies a distinct public state. | Open | SRC-047 |
| GAP-008 | Both platforms | config/status-vocabulary.config.js | Public status presentation | — | Internal status 'Archived' maps to public 'approved' on a reading, not a decision | An agency decision on what this internal status means to a citizen. | The mapping is declared, and its own source records the ambiguity and the alternative reading: Archive is a closure step, and closure in the internal model does not record an outcome. If a matter can be archived without being granted, this needs a distinct public state. | Conflicting | A citizen reading the public label and an officer reading the internal one must be looking at the same thing. | A citizen may be told a matter is at a stage the agency does not consider it to be at. | Misinformed applicants, and complaints founded on a label the agency never intended. | The registry owner. The source itself names docs/audits/DESIGN_AUDIT_BRIEF_ASSESSMENT.md as the point of confirmation. | Business owner | The agency confirms the mapping, or supplies a distinct public state. | Open | SRC-047 |
| GAP-009 | Both platforms | — | — | — | Two status models govern the same records | A stated relationship between the internal lifecycle states and the seven-state public vocabulary. | Both models are declared in configuration. A mapping exists from five internal values to public ones; the lifecycle states are not among those five. | Conflicting | A record moving through the lifecycle must carry a public status at every point, or the public surface shows a stale one. | For most lifecycle states there is no evidenced public status. | A citizen tracking a submission sees a status that does not move while the matter does. | The registry owner, with the platform technical owner. | Business owner | Every lifecycle state carries a declared public status, or the two models are reconciled into one. | Open | SRC-047 SRC-046 |
| GAP-010 | DGO Internal Platform | — | — | — | Three status vocabularies govern the same records, and the widest one has no guard | A declared relationship between the lifecycle states and the stored status values, and a guard over the stored values. | 31 lifecycle states in core/lifecycle.js, used by 4 core file(s); 5 stored values in modules/correspondence.js, carried in full or near-full by 2 workspace module(s) (correspondence, executive) and in single-value form by 10 more where the vocabulary cannot be established from the string; 7 public values. The declared internal-to-public map translates the STORED values, not the lifecycle states. The vocabularies share 1 name(s) (Archived) by coincidence of spelling, not by declaration. | Conflicting | The lifecycle carries the transition guard and the entry preconditions. The vocabulary the operator surface actually uses carries neither, and nothing declares how a record in one is expressed in the other. | The documented lifecycle controls do not demonstrably apply to the status an officer sets in the intake workspace, and a record moving through the lifecycle has no evidenced effect on what a citizen is shown. | A governed transition can be bypassed by writing the stored value directly, and the two can disagree about the same record with nothing to detect it. | The process owner for the correspondence lifecycle, with the platform technical owner. | Process owner | One vocabulary governs, or each value in every vocabulary declares its counterpart in the others and a guard enforces it. | Open | SRC-014 SRC-046 SRC-047 |
| GAP-011 | DGO Internal Platform | config/assignment-cascade.config.js | [PROC-004](detail/PROC-004.md) | — | The authoritative routing matrix is not among the supplied inputs; only the cascade fallback is | The reference data that supplies the live category-to-unit matrix, and confirmation of each mapping, priority and clock in it. | 6 fallback rows, complete and machine-readable, under a key named fallbackMatrix, beside a declared list of the column names the live matrix may arrive under from reference data. | Requires authoritative validation | Routing decides which directorate answers a citizen. It is an operating-model decision the software cannot make, and a fallback is by definition not the decision. | Every routing rule documented here is a fallback. What the estate routes on when the reference data does supply a match is not documented at all, because that data is not present. | Correspondence reaches the wrong unit and the service-level clock starts against the wrong owner. | The registry owner, with the SharePoint reference data the cascade reads its matrix from. | Business owner | The live matrix is supplied, catalogued row by row, and each mapping confirmed against the agency structure. | Open | SRC-048 |
| GAP-012 | DGO Internal Platform | config/assignment-cascade.config.js | — | — | Nothing fires an escalation when a service-level clock expires | An automatic trigger: a scheduled check, a flow, or a rule that raises the escalation when the acknowledgement or completion date passes. | 6 routing rows carrying acknowledgement and completion day counts, which core/assignment-cascade.js turns into dated ack and due fields on the assignment. Breach IS detected — modules/fasttrack.js classifies every tracked item Breached, Due soon or Unassigned against its due date and reports the counts. Escalation IS available — governed actions raise an escalation level, force priority to urgent, write an escalation record, queue an owner notification, and resolve an open escalation. Every one of them requires an operator to press a control. | Partially evidenced | The detection and the response both exist; only the connection between the clock and the response is missing. A clock whose expiry raises nothing is a measurement, not a control. | A breach is visible only to someone who opens the FastTrack workspace and looks. Nothing reaches an owner who does not. | Overdue matters accumulate unseen between visits to the workspace. | The process owner for the correspondence lifecycle. | Operational owner | A scheduled check or a flow raises the escalation and the notification when a clock expires, without waiting for an operator to open a screen. | Open | SRC-048 SRC-019 SRC-049 |
| GAP-014 | Both platforms | config/endpoints.config.js | — | — | Endpoint aliases are not joined to the workflows that answer them | A confirmed alias-to-workflow binding for every alias. | 20 declared aliases, and a partial join covering some of them in docs/reference/portal-endpoint-workflow-ids.json. | Partially evidenced | Without the binding, a documented client call cannot be traced to the documented workflow that serves it. | The traceability chain breaks between the client step and the automated process. | A change to a workflow cannot be assessed against the callers it would affect. | The tenant, through the runtime configuration that supplies each URL. | Technical owner | Every alias names the workflow id that answers it. Trigger URLs themselves must not be recorded: they carry a bearer token. | Open | SRC-003 |
| GAP-015 | DGO Internal Platform | modules/executive.js | [PROC-018](detail/PROC-018.md) | — | A third role authority, derived from the email address and enforced by stylesheet | A statement of which authority governs, and an enforcement that does not rely on presentation. | modules/executive.js derives DGCEO, Officer, EA from substrings of the signed-in email address, independently of the 6 roles and 4 personas in the access model. The panels each sees are separated by 5 stylesheet rule(s) using display:none. | Conflicting | Three authorities decide what a person may do, and the one governing the executive decision panels is neither of the two the access model declares. | The role matrix in this package describes route access. It does not describe who can reach the decision controls inside the executive workspace, which this variant governs instead. | A display rule hides a control; it does not disable it. Anyone whose email does not match still receives the markup and the handlers. | The security or access authority, with the platform technical owner. | Security or access authority | The executive panels are gated by the declared role model, and the gate refuses the action rather than hiding the control. | Open | SRC-018 SRC-052 SRC-034 |
| GAP-016 | DGO Internal Platform | config/rbac.config.js | — | — | An authentication service is provisioned but is not evidenced as enforced | The deployed value of the authentication enable flag, and for each endpoint the trigger authentication type and any server-side role check. | core/auth.js is complete and documents two postures. config/auth.config.js resolves the enable flag to false by default, and states the flag may also be injected at runtime from outside source. In the disabled posture the service returns the local profile as the identity and sends no Authorization header. canAccess() gates every route on a role and persona held in the client profile. | Requires authoritative validation | A control enforced only where the caller controls the code is a usability feature, not a security control. Whether this estate is in that position depends on a runtime value these inputs do not carry. | The access model documented here describes what the interface offers. Whether the estate refuses a caller who bypasses the interface is not established either way. | If the flag is off in the deployed environment, direct calls to the endpoints are not subject to the documented role model. | The tenant owner, from the deployed runtime configuration and the flow trigger authentication settings. | Security or access authority | The deployed enable flag is stated, and for each endpoint the trigger authentication type and the server-side role check are recorded. | Open | SRC-034 SRC-058 SRC-059 |
| GAP-017 | Both platforms | — | — | — | Automated flow estate: no artifact of this class is present | Every action, branch, connector call and exception path of every deployed workflow. | Nothing. The expected path carries no artifact in this repository. | Unavailable | The documentation scope names this class of evidence. Passing over it silently would present a partial estate as a whole one. | No automated process is documented at all: no step, decision, integration call or recovery path. | The integration surface — the only path from either platform to the system of record — is undocumented here. | A tenant export of every workflow in the environment. | Technical owner | An artifact of this class is supplied at docs/reference/flow-contracts/deployed and catalogued. | Open | — |
| GAP-019 | Both platforms | — | — | — | Published integration contracts: no artifact of this class is present | The request and response contract of every call the citizen portal makes. | Nothing. The expected path carries no artifact in this repository. | Unavailable | The documentation scope names this class of evidence. Passing over it silently would present a partial estate as a whole one. | No integration-supported process is documented, and the traceability chain has no contract to anchor a client call to. | A caller and a workflow can disagree about a payload with nothing to arbitrate between them. | The published data contract. | Product owner | An artifact of this class is supplied at docs/deployment/sharepoint/portal-data-contract.json and catalogued. | Open | — |

## Medium priority — 10

| ID | System | Module | Process | Step | Gap | Missing information | Available evidence | Classification | Why required | Impact of absence | Operational risk | Authoritative source | Ownership type missing | Resolution criteria | Status | Sources |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| GAP-013 | DGO Internal Platform | core/retention.js | — | — | Retention periods are declared in code and traced to no authority | The records-management instrument the retention periods derive from. | A frozen table of retention classes with year counts, and a default. | Requires authoritative validation | Retention is a statutory and records-management obligation, not an implementation choice. | Records may be disposed of early, or held longer than the schedule allows. | Non-compliance with the agency records schedule. | The agency records-management authority or its retention schedule. | Business owner | Each class and period cites the instrument it derives from. | Open | SRC-051 |
| GAP-018 | Both platforms | — | — | — | Correspondence Gateway designs: no artifact of this class is present | The designed action graph of each gateway endpoint. | Nothing. The expected path carries no artifact in this repository. | Unavailable | The documentation scope names this class of evidence. Passing over it silently would present a partial estate as a whole one. | The designs behind the citizen-facing endpoints are not documented. | A change to an endpoint cannot be assessed against its design. | The designer paste packages held with the deployment documentation. | Technical owner | An artifact of this class is supplied at docs/deployment/sharepoint/flows/designer-paste/correspondence-gateway and catalogued. | Open | — |
| GAP-020 | Both platforms | — | — | — | Operational execution evidence: no artifact of this class is present | That an endpoint executed, and what status it returned. | Nothing. The expected path carries no artifact in this repository. | Unavailable | The documentation scope names this class of evidence. Passing over it silently would present a partial estate as a whole one. | Nothing is documented about whether any process runs. | Structure is documented with no evidence of operation, which is easily mistaken for assurance. | Captured run records from the tenant. | Operational owner | An artifact of this class is supplied at docs/deployment/sharepoint/evidence and catalogued. | Open | — |
| GAP-021 | Both platforms | — | — | — | System-of-record inventory: no artifact of this class is present | Which lists and libraries exist and which the estate adopted. | Nothing. The expected path carries no artifact in this repository. | Unavailable | The documentation scope names this class of evidence. Passing over it silently would present a partial estate as a whole one. | The system of record is named but its contents are not documented. | A process that writes to a list cannot be traced to the list it writes to. | A survey of the SharePoint environment. | Technical owner | An artifact of this class is supplied at docs/reference/sharepoint-list-index.json and catalogued. | Open | — |
| GAP-023 | Both platforms | — | — | — | Architecture and design materials: no artifact of this class is present | The intended architecture, against which the implemented one can be compared. | Nothing. The expected path carries no artifact in this repository. | Unavailable | The documentation scope names this class of evidence. Passing over it silently would present a partial estate as a whole one. | Implementation is documented with no stated intent to compare it against. | Drift between intent and implementation is undetectable. | The platform architect. | Technical owner | An artifact of this class is supplied at docs/reference/platform-architecture-pack/MASTER_BLUEPRINT.md and catalogued. | Open | — |
| GAP-024 | Both platforms | — | — | — | Test cases assert implementation invariants, not per-process acceptance criteria | For each process, what it is required to do to be accepted. | 1 test files asserting invariants over the implementation. | Partially evidenced | Without acceptance criteria, the implementation is the only definition of correct behaviour. | A process cannot be judged correct or incorrect, only present or absent. | A defect that is consistently implemented, and consistently tested, reads as intended behaviour. | The product owner, with the process owner for each area. | Product owner | Each process names the criteria it must satisfy to be accepted, and a test asserts them. | Open | SRC-060 |
| GAP-025 | Both platforms | — | — | — | Standard operating procedures: no artifact of this class is among the supplied inputs | A written procedure for any process. | Nothing of this class was supplied. | Unavailable | The documentation scope names this class of evidence explicitly. | Every process is documented from implementation alone. Nothing states what any of them is supposed to do, how a person is taught to do it, or what it does in service. | Implementation behaviour is the only available definition of correctness, so a defect that is consistently implemented reads as intended behaviour. | The agency, as the owner of the operating model. | Process owner | An artifact of this class is supplied and its processes are reconciled against the implemented ones. | Open | — |
| GAP-026 | Both platforms | — | — | — | User guides and training materials: no artifact of this class is among the supplied inputs | How an operator is taught to work a process. | Nothing of this class was supplied. | Unavailable | The documentation scope names this class of evidence explicitly. | Every process is documented from implementation alone. Nothing states what any of them is supposed to do, how a person is taught to do it, or what it does in service. | Implementation behaviour is the only available definition of correctness, so a defect that is consistently implemented reads as intended behaviour. | The agency, as the owner of the operating model. | Operational owner | An artifact of this class is supplied and its processes are reconciled against the implemented ones. | Open | — |
| GAP-027 | Both platforms | — | — | — | Support and operational records: no artifact of this class is among the supplied inputs | What actually goes wrong in service, and how it is resolved. | Nothing of this class was supplied. | Unavailable | The documentation scope names this class of evidence explicitly. | Every process is documented from implementation alone. Nothing states what any of them is supposed to do, how a person is taught to do it, or what it does in service. | Implementation behaviour is the only available definition of correctness, so a defect that is consistently implemented reads as intended behaviour. | The agency, as the owner of the operating model. | Support owner | An artifact of this class is supplied and its processes are reconciled against the implemented ones. | Open | — |
| GAP-028 | Both platforms | — | — | — | User journey records: no artifact of this class is among the supplied inputs | What a citizen or an officer experiences end to end. | Nothing of this class was supplied. | Unavailable | The documentation scope names this class of evidence explicitly. | Every process is documented from implementation alone. Nothing states what any of them is supposed to do, how a person is taught to do it, or what it does in service. | Implementation behaviour is the only available definition of correctness, so a defect that is consistently implemented reads as intended behaviour. | The agency, as the owner of the operating model. | Product owner | An artifact of this class is supplied and its processes are reconciled against the implemented ones. | Open | — |

## Low priority — 2

| ID | System | Module | Process | Step | Gap | Missing information | Available evidence | Classification | Why required | Impact of absence | Operational risk | Authoritative source | Ownership type missing | Resolution criteria | Status | Sources |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| GAP-002 | DGO Internal Platform | — | — | — | Some governed writes cannot be named from their call site | The action name at the call site, for the modules that pass it as a variable. | 4 module(s) — activities, executive, fasttrack, user-admin — call executeOwnedAction() with the action passed as a variable. Their actions are recovered from the per-action table and, where the name also appears as a literal in the module, from the boundary charter. Every recovered step is classified Inferred and says which recovery produced it. | Inferred | A step recovered by reading two artifacts together is weaker evidence than one read from a single call site, and the difference should not be invisible to a reader. | For these modules, the mapping from a control the operator presses to the action it raises is a reading rather than a fact. | An action could be raised that no artifact declares, and this documentation would not show it. | The module source, if the call sites are changed to name their actions literally. | Technical owner | Each call site names its action literally, or the module declares the set of names it dispatches. | Open | SRC-006 SRC-018 SRC-019 SRC-033 |
| GAP-022 | Both platforms | — | — | — | Delivery register: no artifact of this class is present | What the delivery team already records as outstanding. | Nothing. The expected path carries no artifact in this repository. | Unavailable | The documentation scope names this class of evidence. Passing over it silently would present a partial estate as a whole one. | This register and the delivery register may disagree about what is open. | An item closed in one place and open in another. | The delivery owner. | Product owner | An artifact of this class is supplied at docs/deployment/sharepoint/OPEN_ITEMS.md and catalogued. | Open | — |

## Conflicts

Where sources disagree, both readings are recorded, the authority and currency of each is
assessed, and neither is silently preferred. Each row names the validation that would settle it.

### TRAN-053 — internal 'Accepted' → public 'review'

| Aspect | Record |
| --- | --- |
| Conflicting sources | `SRC-047` config/status-vocabulary.config.js |
| Reading the implementation takes | The mapping is declared, and its own source records this particular edge as a reading rather than a fact, flagged for agency confirmation. |
| Alternative reading, as its own source states it | "Accepted" means the registry accepted the item for handling, which is the public "Under review". If it instead means the request itself was granted, it maps to "approved". |
| Source authority | Both readings come from the same artifact, so neither is more current than the other and neither can settle the question. The artifact is authoritative for what the software DOES — it is the map the renderer calls — and is explicitly not authoritative for what the status MEANS to the agency, which it defers. |
| Currency | Both readings are carried by the same artifact at the same revision, so neither is the more recent. |
| Implementation relevance | Implemented: the declared edge is what governedStatusLabel() returns today. A citizen is already being shown 'review' for a record the platform holds as 'Accepted'. |
| Validation required | The registry owner states which reading is intended. If the alternative is intended, the map changes and, for the archived case, a distinct public state is needed. |
| Authority to resolve | Named in the gap register row for this subject. |

### TRAN-056 — internal 'Archived' → public 'approved'

| Aspect | Record |
| --- | --- |
| Conflicting sources | `SRC-047` config/status-vocabulary.config.js |
| Reading the implementation takes | The mapping is declared, and its own source records this particular edge as a reading rather than a fact, flagged for agency confirmation. |
| Alternative reading, as its own source states it | Archive is a closure step, and closure in the internal model does not record an outcome. If a matter can be archived without being granted, this needs a distinct public state. |
| Source authority | Both readings come from the same artifact, so neither is more current than the other and neither can settle the question. The artifact is authoritative for what the software DOES — it is the map the renderer calls — and is explicitly not authoritative for what the status MEANS to the agency, which it defers. |
| Currency | Both readings are carried by the same artifact at the same revision, so neither is the more recent. |
| Implementation relevance | Implemented: the declared edge is what governedStatusLabel() returns today. A citizen is already being shown 'approved' for a record the platform holds as 'Archived'. |
| Validation required | The registry owner states which reading is intended. If the alternative is intended, the map changes and, for the archived case, a distinct public state is needed. |
| Authority to resolve | Named in the gap register row for this subject. |

### GAP-001 — Two artifacts authorise a governed action, and only one of them says what the action does

| Aspect | Record |
| --- | --- |
| Conflicting sources | `SRC-035` config/action-ownership.config.js<br>`SRC-005` config/module-boundaries.config.js<br>`SRC-037` core/action-authority.js |
| Reading the implementation takes | core/action-authority.js accepts an action when the per-action table carries a spec for it OR the module boundary charter lists it in owns[]. The table carries 61 specs, each naming an owner, a service, an audit vocabulary and a backend requirement. The charter declares 124 capability names across 29 modules, of which 82 have no per-action spec. The charter list mixes writes with view capabilities, so its entries cannot be read as actions without checking each one. |
| Alternative reading, as its own source states it | core/action-authority.js accepts an action when the per-action table carries a spec for it OR the module boundary charter lists it in owns[]. The table carries 61 specs, each naming an owner, a service, an audit vocabulary and a backend requirement. The charter declares 124 capability names across 29 modules, of which 82 have no per-action spec. The charter list mixes writes with view capabilities, so its entries cannot be read as actions without checking each one. |
| Source authority | The artifact states the fact and is not authoritative for it; the gap register row for this subject names who is. |
| Currency | Not separately assessable: the disagreement is between an artifact and an absence, not between two dated artifacts. |
| Implementation relevance | The declared reading is the one the software acts on today. |
| Validation required | Every name the runtime will accept as an action has a per-action record, or the charter states explicitly which of its entries are actions and which are views. |
| Authority to resolve | The platform technical owner, as the owner of both configurations. |

### GAP-007 — Internal status 'Accepted' maps to public 'review' on a reading, not a decision

| Aspect | Record |
| --- | --- |
| Conflicting sources | `SRC-047` config/status-vocabulary.config.js |
| Reading the implementation takes | The mapping is declared, and its own source records the ambiguity and the alternative reading: "Accepted" means the registry accepted the item for handling, which is the public "Under review". If it instead means the request itself was granted, it maps to "approved". |
| Alternative reading, as its own source states it | The mapping is declared, and its own source records the ambiguity and the alternative reading: "Accepted" means the registry accepted the item for handling, which is the public "Under review". If it instead means the request itself was granted, it maps to "approved". |
| Source authority | The artifact states the fact and is not authoritative for it; the gap register row for this subject names who is. |
| Currency | Not separately assessable: the disagreement is between an artifact and an absence, not between two dated artifacts. |
| Implementation relevance | The declared reading is the one the software acts on today. |
| Validation required | The agency confirms the mapping, or supplies a distinct public state. |
| Authority to resolve | The registry owner. The source itself names docs/audits/DESIGN_AUDIT_BRIEF_ASSESSMENT.md as the point of confirmation. |

### GAP-008 — Internal status 'Archived' maps to public 'approved' on a reading, not a decision

| Aspect | Record |
| --- | --- |
| Conflicting sources | `SRC-047` config/status-vocabulary.config.js |
| Reading the implementation takes | The mapping is declared, and its own source records the ambiguity and the alternative reading: Archive is a closure step, and closure in the internal model does not record an outcome. If a matter can be archived without being granted, this needs a distinct public state. |
| Alternative reading, as its own source states it | The mapping is declared, and its own source records the ambiguity and the alternative reading: Archive is a closure step, and closure in the internal model does not record an outcome. If a matter can be archived without being granted, this needs a distinct public state. |
| Source authority | The artifact states the fact and is not authoritative for it; the gap register row for this subject names who is. |
| Currency | Not separately assessable: the disagreement is between an artifact and an absence, not between two dated artifacts. |
| Implementation relevance | The declared reading is the one the software acts on today. |
| Validation required | The agency confirms the mapping, or supplies a distinct public state. |
| Authority to resolve | The registry owner. The source itself names docs/audits/DESIGN_AUDIT_BRIEF_ASSESSMENT.md as the point of confirmation. |

### GAP-009 — Two status models govern the same records

| Aspect | Record |
| --- | --- |
| Conflicting sources | `SRC-047` config/status-vocabulary.config.js<br>`SRC-046` core/lifecycle.js |
| Reading the implementation takes | Both models are declared in configuration. A mapping exists from five internal values to public ones; the lifecycle states are not among those five. |
| Alternative reading, as its own source states it | Both models are declared in configuration. A mapping exists from five internal values to public ones; the lifecycle states are not among those five. |
| Source authority | The artifact states the fact and is not authoritative for it; the gap register row for this subject names who is. |
| Currency | Not separately assessable: the disagreement is between an artifact and an absence, not between two dated artifacts. |
| Implementation relevance | The declared reading is the one the software acts on today. |
| Validation required | Every lifecycle state carries a declared public status, or the two models are reconciled into one. |
| Authority to resolve | The registry owner, with the platform technical owner. |

### GAP-010 — Three status vocabularies govern the same records, and the widest one has no guard

| Aspect | Record |
| --- | --- |
| Conflicting sources | `SRC-014` modules/correspondence.js<br>`SRC-046` core/lifecycle.js<br>`SRC-047` config/status-vocabulary.config.js |
| Reading the implementation takes | 31 lifecycle states in core/lifecycle.js, used by 4 core file(s); 5 stored values in modules/correspondence.js, carried in full or near-full by 2 workspace module(s) (correspondence, executive) and in single-value form by 10 more where the vocabulary cannot be established from the string; 7 public values. The declared internal-to-public map translates the STORED values, not the lifecycle states. The vocabularies share 1 name(s) (Archived) by coincidence of spelling, not by declaration. |
| Alternative reading, as its own source states it | 31 lifecycle states in core/lifecycle.js, used by 4 core file(s); 5 stored values in modules/correspondence.js, carried in full or near-full by 2 workspace module(s) (correspondence, executive) and in single-value form by 10 more where the vocabulary cannot be established from the string; 7 public values. The declared internal-to-public map translates the STORED values, not the lifecycle states. The vocabularies share 1 name(s) (Archived) by coincidence of spelling, not by declaration. |
| Source authority | The artifact states the fact and is not authoritative for it; the gap register row for this subject names who is. |
| Currency | Not separately assessable: the disagreement is between an artifact and an absence, not between two dated artifacts. |
| Implementation relevance | The declared reading is the one the software acts on today. |
| Validation required | One vocabulary governs, or each value in every vocabulary declares its counterpart in the others and a guard enforces it. |
| Authority to resolve | The process owner for the correspondence lifecycle, with the platform technical owner. |

### GAP-015 — A third role authority, derived from the email address and enforced by stylesheet

| Aspect | Record |
| --- | --- |
| Conflicting sources | `SRC-018` modules/executive.js<br>`SRC-052` styles/app.css<br>`SRC-034` config/rbac.config.js |
| Reading the implementation takes | modules/executive.js derives DGCEO, Officer, EA from substrings of the signed-in email address, independently of the 6 roles and 4 personas in the access model. The panels each sees are separated by 5 stylesheet rule(s) using display:none. |
| Alternative reading, as its own source states it | modules/executive.js derives DGCEO, Officer, EA from substrings of the signed-in email address, independently of the 6 roles and 4 personas in the access model. The panels each sees are separated by 5 stylesheet rule(s) using display:none. |
| Source authority | The artifact states the fact and is not authoritative for it; the gap register row for this subject names who is. |
| Currency | Not separately assessable: the disagreement is between an artifact and an absence, not between two dated artifacts. |
| Implementation relevance | The declared reading is the one the software acts on today. |
| Validation required | The executive panels are gated by the declared role model, and the gate refuses the action rather than hiding the control. |
| Authority to resolve | The security or access authority, with the platform technical owner. |

## Missing ownership, by type

| Ownership type | Gaps naming it |
| --- | --- |
| Business owner | 5 |
| Product owner | 4 |
| Process owner | 6 |
| Technical owner | 7 |
| Operational owner | 3 |
| Support owner | 1 |
| Approval authority | 0 |
| Security or access authority | 2 |
